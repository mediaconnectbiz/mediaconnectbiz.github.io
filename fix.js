$(document).ready(function() {
/*    var header =  document.querySelector('header');
    var initPos = header.getBoundingClientRect().top + window.pageYOffset;
    var placeholder = null;

    window.addEventListener('scroll', function() {
        var pageScroll = window.pageYOffset;
        var parentElem =  header.parentElement;

        var blockHeight = getComputedStyle(header, '').getPropertyValue('height');
        var s = 'height: ' + blockHeight;

        if (pageScroll >= initPos) {
            $(header).addClass('sticky');

            if (!placeholder) {
                placeholder = document.createElement('div');
                placeholder.style.cssText = s;
                placeholder.className = 'placeholder';
                parentElem.insertBefore(placeholder,  header);
            } else {
                placeholder.style.cssText = s;
            }

        } else {
            placeholder = $('.placeholder')[0];
            if (placeholder) {
                placeholder.style.cssText = 'position: absolute;';
            }
            if (header) {
                header.classList.remove('sticky');
            }
        }

    }, false);*/
});